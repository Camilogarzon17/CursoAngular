import { AltercharfornumberPipe } from './altercharfornumber.pipe';

describe('AltercharfornumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AltercharfornumberPipe();
    expect(pipe).toBeTruthy();
  });
});
